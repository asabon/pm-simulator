# PM Simulator - AI開発ガイドライン & 開発ハーネス規約

本ドキュメントは、Google Antigravity が本リポジトリで作業する際に遵守すべき開発プロセス・コーディング規約・Git運用・自律的行動規範を定義します。

---

## 1. 開発プロセス & ペアプログラミング方針

- **対話と段階的な進展**:
  - 実装前にユーザーと仕様・設計（ゲームデザイン、UI構成、コアループ、アーキテクチャ）を相談・すり合わせる。
  - 一度に大きく作らず、小さく動作確認できる単位で段階的に実装を進める。
  - 大きな変更や新機能の追加時は、まず実装計画（Implementation Plan）を提示してユーザーの承認を得るプロセスを徹底する。
- **ドキュメントの整合性と仕様化優先**:
  - 仕様に関する合意が得られたら、コード実装前に `docs/spec/` を必ず最新の状態に更新すること。コードと仕様書の間に乖離を作らない。
- **変更の確認**:
  - コミット前に変更対象ファイル・ステータスを確認し、クリーンな状態を維持する。

---

## 2. プロジェクト開発 & コーディング規約

- **JavaScript (Webプロトタイプ)**:
  - コード変更時は **必ずコミット前・ユーザーへの完了報告前に `npm test` (Vitest) および `npm run lint` (ESLint) を実行** し、ユニットテストの通過およびエラー・警告がないことを確認すること。
  - オブジェクト指向およびモジュール化の明確な責務分け（データモデル、サービス層、UI層、ゲームエンジン）を意識すること。
- **Godot 4 (本格GUI開発フェーズ・将来予定)**:
  - GDScript のスタイルガイドに準拠し、静的型付けアノテーションを徹底すること。
- **環境差異・不要ファイルの排除**:
  - `.vscode/`、node_modules、自動生成ファイル等の環境依存・一時ファイルはコミット対象外を徹底する。

---

## 3. Git / GitHub 開発ワークフロー & コミット規約

- **ブランチ戦略 (GitHub Flow)**:
  - `main` ブランチ: 常に安定して動作する状態を維持する。直接の `commit` および `push` は禁止。
  - トピックブランチ: 作業目的に応じたプレフィックスを付け、`<タイプ>/<3桁番号>-<概要>` の形式で作成する（例: `feature/001-cafe-commands`）。

| プレフィックス | 用途・選定基準 | 具体例 |
| :--- | :--- | :--- |
| **`feature/`** | 新機能の追加・新規仕様の実装 | `feature/001-cafe-commands` |
| **`fix/`** | バグや不具合の修正 | `fix/005-status-calc-bug` |
| **`refactor/`** | 仕様を変えない構造改善・リファクタリング | `refactor/008-extract-event-engine` |
| **`docs/`** | ドキュメント類（仕様書、README、設計等）の追加・修正 | `docs/002-cafe-menu-spec` |
| **`chore/`** | ビルド設定・依存関係更新・保守作業 | `chore/004-upgrade-vitest` |
| **`test/`** | 単体テスト等の追加・更新 | `test/006-engine-tests` |

- **Git Hooks による誤操作防止**:
  - 本リポジトリでは `.githooks/` 配下に `pre-commit` および `pre-push` を用意し、`main` への直接操作をブロックする。
  - 初期設定コマンド: `git config core.hooksPath .githooks`
- **プルリクエスト (PR) 運用**:
  - 作業完了後は GitHub 上で Pull Request を作成し、レビュー・動作確認を経て `main` へマージ（Squash and merge または Rebase and merge 推奨）する。
  - PR 作成時は [`.github/pull_request_template.md`](.github/pull_request_template.md) のフォーマットを適用し、タイトルは `[種別] Issue #XXX: 概要` とする。
  - PR 発行には GitHub CLI (`gh pr create`) または `.agents/skills/create-pr/` スキルを活用する。
- **コミット単位 & メッセージ**:
  - 1つの論理的な変更ごとに小さな単位でコミットする。
  - コミットメッセージは**日本語**で、変更内容が明確にわかるように記述する。

---

## 4. Issue 駆動開発ワークフロー (`docs/issues/`)

あらゆる変更（新機能開発、バグ修正、リファクタリング、ドキュメント更新、保守作業 `chore` を含む）において、**例外なく以下のステップ順序に従って作業を進める**。

### 標準作業フロー（5ステップ）
1. **Issue の起票**:
   - 作業開始前に必ず `docs/issues/<3桁連番>-<概要>.md` を作成する（例: `001-cafe-commands.md`）。
   - 雛形として [`docs/issues/TEMPLATE.md`](docs/issues/TEMPLATE.md) を使用し、目的と受け入れ基準（Acceptance Criteria）を明記する。
   - ※ Issue 作成時は `.agents/skills/create-issue/` スキルを活用する。
2. **トピックブランチの作成**:
   - 起票した Issue 番号に基づき、`<タイプ>/<3桁連番>-<概要>` のブランチを作成して切り替える（例: `feature/001-cafe-commands`, `docs/002-cafe-menu-spec`）。
3. **実装 & 小単位コミット**:
   - 定義した受け入れ基準を満たす実装・テストを行い、小さな単位でコミットする。
4. **受け入れ基準の検証と Issue 完了更新**:
   - 動作確認を行い、Issue ファイルの受け入れ基準チェックボックスを埋め、ステータスを `完了` に更新してコミットする。
5. **Pull Request (PR) の発行 ＆ CI検証**:
   - リモートへプッシュ後、`.agents/skills/create-pr/` スキルを使用して PR を発行し、GitHub Actions CI の成功を確認する。

---

## 🤖 AIエージェント必須ガードレール ＆ 自動完遂ルール

### 【AI必須ガードレール】ファイル変更着手前のブランチ自動検証 ＆ 選択判定
- ユーザーからコード修正・テキスト変更・仕様変更リクエストを受けた際、**ファイルを1文字でも編集する前に、必ず最初のツール実行として `git branch --show-current` を確認すること。**
- **`main` ブランチにいる場合**: 直ちに Issue 起票（または専用トピックブランチ作成）を行い、ブランチを切り替えてから作業を開始すること。
- **作業ブランチ上にいる場合**:
  - 依頼内容が既存作業の続き・同文脈の修正・CI対応 ➔ **現在の作業ブランチを維持**する。
  - 依頼内容が既存作業とは異なる新規機能・別文脈の修正 ➔ **`main` に戻って最新化の上、新しい作業ブランチを作成**すること。

### 【AI必須ルール】議論確定からPR発行・CI検証までの一連作業自動完遂 (End-to-End Auto PR & CI Verification)
- ユーザーからの相談・議論を経て修正内容や実装計画が確定し、**作業を開始した後は、ファイル修正・テスト検証から `/create-pr` による PR 発行および GitHub Actions CI の成功確認までを途中で止めず、一連の作業として自動で完遂すること。**
- PR発行後に必ず `gh pr checks` 等で CI 実行結果を確認し、成功（Pass）を確認した上でユーザーへ報告すること。

### 作業中の別要件・スコープ管理ルール
トピックブランチで作業中に、別件の相談・新規アイデア・バグ報告が発生した場合は以下の通り対応する：
- **無関係な変更の混入禁止**: 現在の Issue スコープ外の変更を同一ブランチに勝手に含めてはならない。
- **ユーザーへの確認と選択肢の提示**:
  1. **仕様変更/改善（スコープ内）**: 現在の Issue の受け入れ基準を更新して同一ブランチで実装。
  2. **新規アイデア/別要件（通常）**: `docs/issues/` に新 Issue を起票し、まずは現在の作業を完了・マージさせる（推奨）。
  3. **緊急の別要件**: 現在の作業を退避（コミットまたは stash）し、`main` から新トピックブランチを作成して優先対応。

---

## 📂 フォルダ構成のルール
- 仕様書は `docs/spec/` 配下に格納する。
- 過去バージョンアーカイブは `docs/old/` 配下に格納する。
- 課題・Issue管理は `docs/issues/` 配下に格納する。
- Webプロトタイプは `web/` 配下に格納する。
- カスタムスキルは `.agents/skills/<skill_name>/SKILL.md` に定義する。
- Godot関連のスクリプトは将来的に `scenes/` または `src/` 配下に構造化して配置する。
